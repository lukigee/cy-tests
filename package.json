{
  "scripts": {
    "open": "yarn run cypress open",
    "start": "yarn cypress run --config videoRecording=false"
  },
  "devDependencies": {
    "cypress": "^9.2.0",
    "typescript": "^4.5.4"
  }
}
